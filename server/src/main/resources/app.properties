prometheus.endpointPortNumber=9999

# Ring buffer sizes for the mediator and notifier
#mediator.ringBufferSize=
#notifier.ringBufferSize=
#mediator.ringBufferSize=1048576

# too much?
#mediator.ringBufferSize=67108864

# seemed to work with simulator: blockStream.delayBetweenBlockItems=10000
#mediator.ringBufferSize=33554432
mediator.ringBufferSize=8388608
notifier.ringBufferSize=2048

# Timeout for consumers to wait for block item before timing out. Default is 1500 milliseconds.
consumer.timeoutThresholdMillis=40000
producer.observerType=NOOP
#mediator.mediatorType=NOOP
#persistence.storage.persistenceType=NOOP
